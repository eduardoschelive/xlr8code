spring:
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:6543/xlr8code
    username: two-forty
    password: forty-two
    driverClassName: org.postgresql.Driver

jwt:
  secret-key: secret
  expiration-time: 1

logging:
  level:
    org:
      springframework:
        transaction:
          interceptor:
            TRACE
